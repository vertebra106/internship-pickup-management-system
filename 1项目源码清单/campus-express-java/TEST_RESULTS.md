# 实际测试结果

验证日期：2026-09-19，Windows 环境。

## 环境与命令

- `java -version`：Java 26.0.2.1，Java HotSpot 64-Bit Server VM。
- `javac -version`：26.0.2.1。
- 编译使用 `--release 17 -encoding UTF-8`，主程序与测试程序编译均成功。
- 最终执行 `test.ps1`：退出码 0，`RESULT: 29 passed, 0 failed`。
- 另执行 `'0' | powershell -NoProfile -ExecutionPolicy Bypass -File .\run.ps1`：编译成功，显示数据文件路径与菜单，正常退出，退出码 0。
- 未在 JDK 17 / 21 实际运行时或 IntelliJ 图形界面内测试；Java 17 兼容性依据为 release 17 编译检查。

## 通过的 29 组测试

1. 首次启动无文件：空列表且允许写入。
2. 正常新增：待取件状态、电话前导零。
3. 拒绝重复单号（包含首尾空格归一化）。
4. 拒绝重复取件码。
5. 两种查询以及不存在记录查询。
6. 修改取件码冲突：拒绝且内存、磁盘内容不变。
7. 修改时允许保留自己的码，也允许换新码。
8. 取件后状态立即保存。
9. 拒绝重复取件。
10. 已取件记录修改资料不重置状态。
11. 已取件记录继续占用取件码。
12. 新建 Service 重新读取完整字段，包含中文、前导零、逗号、双引号。
13. 不存在记录的修改、删除、取件均拒绝。
14. 拒绝 null、空白、非法电话和过长字段。
15. 拒绝制表符、CR/LF、NUL、Unicode 行分隔符。
16. 修改查询返回列表不影响内部数据。
17. 菜单、查询方式与空输入有反馈。
18. 取消删除后数据仍在。
19. 确认删除后内存和磁盘均删除。
20. 输入中途结束不提交半条记录。
21. 零字节文件正常读取。
22. 坏行、重复单号/取件码、未知状态、空字段被跳过；后续有效行仍可查询；新增、修改、删除、取件均被只读模式阻止；原文件不变。
23. 将数据路径设为目录制造读取异常，提示并进入只读。
24. 非法 UTF-8 不导致未处理异常，进入只读。
25. 保存父目录被普通文件占用：新增、修改、删除、取件全部失败且内存不变；UI 不误报成功；恢复路径后可重试成功。
26. 删除最后一条后生成空文件，重新读取为空。
27. 目标被非空目录占用，原子替换失败：内存保持待取件，旧备份不变，临时文件清理完成。
28. 界面输入校验失败后，可以继续正常新增及查看。
29. 启动三个独立 JVM：第一次新增中文记录并退出；第二次重启查询、修改取件码、取件、重复取件并退出；第三次重启确认新姓名、新取件码、已取件状态仍在。

## 独立进程证据

本轮测试保留的临时目录：

```text
C:\Users\10536\AppData\Local\Temp\campus-express-tests-11687949151217055612
```

其中 `process-demo/first-output.txt`、`second-output.txt`、`third-output.txt` 保存三次实际输出，相应 `*-input.txt` 保存输入。第三次进程输出的记录为：

```text
单号：P001 | 收件人：新姓名 | 电话：01234567890 | 取件码：p2 | 公司：新公司 | 状态：已取件
```

这些临时文件可能被系统后续清理，可随时重跑 `test.ps1` 生成新的证据目录。项目真实 `data` 目录没有预置演示记录，也没有被自动化测试改写。

## 未验证边界

没有实际制造断电、磁盘满、真实 ACL 权限拒绝或不支持原子移动的文件系统；没有并发、性能、压力或安全审计测试。保存失败测试使用确定性的路径故障，不把它等同于覆盖所有操作系统错误。测试组数不代表代码覆盖率百分比。
